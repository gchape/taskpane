"use client";
import {type JSX} from "react";
import NavBar from "./components/navbar/NavBar.tsx";

const App = (): JSX.Element => {
    return (
        <div>
            <NavBar/>
        </div>
    );
};

export default App;